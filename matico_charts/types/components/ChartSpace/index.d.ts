import { ChartSpaceSpec } from "../types";
export default function ChartSpace({ xAxis, yAxis, xLabel, yLabel, title, subtitle, attribution, dimensions, categorical, ...rest }: ChartSpaceSpec): JSX.Element;
