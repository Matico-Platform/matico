import styled from 'styled-components';

const QueryBrowser = styled.div``;
const QueryList = styled.ul``;
const Query = styled.li``;

export const Styles = {
    QueryBrowser,
    QueryList,
    Query,
};
