import styled from 'styled-components';

const ColorPaletteSelector = styled.div`
    display: flex;
    flex-direction: row;
    align-items: center;

    svg {
        cursor: pointer;
    }
`;
export const Styles = {
    ColorPaletteSelector,
};
